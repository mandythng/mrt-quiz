image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html app.js style.css mrt_data.js map.svg public/
  artifacts:
    paths:
      - public
  only:
    - main
    - master
